.venv/
.idea/
__pycache__/
*.pyc
config/api.txt
.claude/

# 원본 데이터 (대용량) — user_detail.csv는 호스팅용으로 예외
data/raw/
!data/raw/user_detail.csv

# 중간 산출물 (쇼케이스 전처리 대용량만 제외)
# clustered_users.csv / survival_data.csv 는 호스팅용으로 포함
data/processed/showcase/daily_segment_processed.csv

# 로그
*.log
*.txt
!requirements.txt

# 리포트 (초안 문서)
exp_analysis/reports/
user_clustering/reports/
reports/
